<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#424242">

    <ImageView
        android:id="@+id/logoTrackParcelPage"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:src="@drawable/white_trans"
        android:contentDescription="Logo" />



    <EditText
        android:id="@+id/editTextTrackingId"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:hint="Enter Your Parcel Tracking ID"
        android:background="@drawable/rounded_input"
        android:layout_below="@id/logoTrackParcelPage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="150dp"
        android:textColor="#000000"
        android:padding="10dp" />

    <Button
        android:id="@+id/btnSearch"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:text="Search"
        android:background="@drawable/rounded_button"
        android:textColor="#FFFFFF"
        android:layout_below="@id/editTextTrackingId"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp" />
</RelativeLayout>
